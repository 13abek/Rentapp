@model IEnumerable<Online13.Models.TeacherContactData>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

    <h2>Change the requirde information</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UpperTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecondTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThirdTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UpperTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecondTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ThirdTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Update", new { id=item.Id }) |
            <a href="@Url.Action("Delete","TeacherContactData",new { id=item.Id})" onclick="return confirm('Are you sure?')">Delete</a>
        </td>
    </tr>
}

</table>
