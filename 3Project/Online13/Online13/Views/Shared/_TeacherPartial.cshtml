@model List<Online13.Models.Teacher>

    @foreach (var item in Model)
    {
        <div class="col-md-3 col-sm-4 col-xs-12">
            <div class="single-teacher">
                <div class="single-teacher-img">
                    <a href="teacher-details.html"><img src="~/Uploads/@item.Image" alt="image"></a>
                </div>
                <div class="single-teacher-content text-center">
                    <h2><a href="@Url.Action("TeacherDetails","Teacher", new { id=item.Id})">@item.FullName</a></h2>
                    <h4>@item.Position.Name</h4>
                    <ul>
                        @foreach (var social in item.SocialTeachers)
                        {
                            <li><a href="@social.Link"><i class="@social.Icon"></i></a></li>
                        }

                    </ul>
                </div>
            </div>
        </div>
    }

